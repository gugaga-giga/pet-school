<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.petschool.ai.mapper.ChatSessionMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_chat_session (user_id, title, created_at, updated_at)
        VALUES (#{userId}, #{title}, #{createdAt}, #{updatedAt})
    </insert>

    <select id="selectById" resultType="com.petschool.ai.entity.ChatSession">
        SELECT id, user_id, title, created_at, updated_at
        FROM ai_chat_session WHERE id = #{id}
    </select>

    <select id="selectByUserId" resultType="com.petschool.ai.entity.ChatSession">
        SELECT id, user_id, title, created_at, updated_at
        FROM ai_chat_session WHERE user_id = #{userId}
        ORDER BY updated_at DESC
    </select>

    <update id="update">
        UPDATE ai_chat_session
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="updatedAt != null">updated_at = #{updatedAt},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM ai_chat_session WHERE id = #{id}
    </delete>

</mapper>
