<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.petschool.mapper.MessageMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO message (user_id, title, content, is_read)
        VALUES (#{userId}, #{title}, #{content}, #{isRead})
    </insert>
    <select id="selectByUserId" resultType="com.petschool.entity.Message">
        SELECT id, user_id, title, content, is_read, create_time
        FROM message WHERE user_id = #{userId} ORDER BY create_time DESC
    </select>
    <update id="updateRead">
        UPDATE message SET is_read = 1 WHERE id = #{id}
    </update>
</mapper>
