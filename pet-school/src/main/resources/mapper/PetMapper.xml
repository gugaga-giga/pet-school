<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.petschool.mapper.PetMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO pet (user_id, name, breed, age, weight)
        VALUES (#{userId}, #{name}, #{breed}, #{age}, #{weight})
    </insert>
    <select id="selectById" resultType="com.petschool.entity.Pet">
        SELECT id, user_id, name, breed, age, weight FROM pet WHERE id = #{id}
    </select>
    <select id="selectByUserId" resultType="com.petschool.entity.Pet">
        SELECT id, user_id, name, breed, age, weight FROM pet WHERE user_id = #{userId}
    </select>
</mapper>
