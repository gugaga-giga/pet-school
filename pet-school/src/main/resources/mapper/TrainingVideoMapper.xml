<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.petschool.mapper.TrainingVideoMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO training_video (record_id, video_url, cover_url)
        VALUES (#{recordId}, #{videoUrl}, #{coverUrl})
    </insert>
    <select id="selectByRecordId" resultType="com.petschool.entity.TrainingVideo">
        SELECT id, record_id, video_url, cover_url, upload_time
        FROM training_video WHERE record_id = #{recordId} ORDER BY upload_time DESC
    </select>
</mapper>
